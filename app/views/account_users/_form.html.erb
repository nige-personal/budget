<%= form_with(model: account_user, local: true) do |form| %>
  <% if account_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account_user.errors.count, "error") %> prohibited this account_user from being saved:</h2>

      <ul>
      <% account_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :account_id %>
       <%= form.collection_select :account_id, @user_accounts, :id, :name, {:include_blank => 'Select an Account'}%>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select :user_id, @users, :id, :email %>
  </div>

  <div class="field">
    <%= form.label :privilege %>
    <%= form.text_field :privilege, id: :account_user_privilege %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
