<%= form_with(model: group_category, local: true) do |form| %>
  <% if group_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_category.errors.count, "error") %> prohibited this group_category from being saved:</h2>

      <ul>
      <% group_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :group_category_category_id %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.text_field :group_id, id: :group_category_group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
